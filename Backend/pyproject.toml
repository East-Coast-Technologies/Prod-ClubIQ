[project]
name = "backend"
version = "0.1.0"
description = "ClubIQ backend service"
readme = { text = "ClubIQ backend service", content-type = "text/plain" }
requires-python = ">=3.13"
dependencies = [
    "flask-cors>=6.0.3",
    "flask-sqlalchemy>=3.1.1",
    "ruff>=0.15.17",
]
